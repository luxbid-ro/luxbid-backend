# Production Environment Variables
# Copy to .env in production environment

# Database (get from Render PostgreSQL dashboard)
DATABASE_URL="postgresql://luxbid:password@dpg-xxxxx-a.oregon-postgres.render.com/luxbid"

# JWT (generate secure secret)
JWT_SECRET="luxbid-jwt-secret-production-2024-very-secure-random-key"
JWT_EXPIRES_IN="7d"

# Server
PORT=4000
NODE_ENV="production"

# CORS (allow production domains)
CORS_ORIGIN="https://luxbid.ro,https://www.luxbid.ro"
